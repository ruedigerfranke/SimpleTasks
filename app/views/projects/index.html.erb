<div class="container mx-auto p-4">
  <p id="notice"><%= notice %></p>
  <h1 class="text-2xl mb-2">Your Projects</h1>
  <% @projects.each do |project| %>
    <%= render project %>
  <% end %>

  <div class="mt-3">
    <%= link_to 'New Project', new_project_path, class: 'btn btn-md btn-secondary' %>
  </div>
</div>
